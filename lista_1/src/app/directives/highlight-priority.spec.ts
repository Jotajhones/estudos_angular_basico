import { HighlightPriority } from './highlight-priority';

describe('HighlightPriority', () => {
  it('should create an instance', () => {
    const directive = new HighlightPriority();
    expect(directive).toBeTruthy();
  });
});
